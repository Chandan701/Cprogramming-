#include<stdio.h>

int main(){
	   char str[100]="Hi Chandan";
	   int len=strlen(str);   {
	   //calling str length with str as argument
	   printf("Length of the String is%d",len);
		  }
	     return 0;
}
